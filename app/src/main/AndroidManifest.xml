<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lab.itsoul.com.dailygoods.app.sayedandroidlearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".navigationDashBoard"
            android:label="@string/title_activity_navigation_dash_board"
            android:theme="@style/AppTheme.NoActionBar">


        </activity>

        <activity android:name=".Dashboard"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SharedPreferenceLearning">

        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".CountryProfileActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>